{
  "name": "personal-blog",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "npm run build && wrangler dev",
    "test": "node --test src/utils/projects.test.js",
    "astro": "astro",
    "deploy": "npm run build && wrangler deploy",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "@astrojs/cloudflare": "12",
    "@astrojs/rss": "^4.0.12",
    "astro": "^5.7.0"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "wrangler": "^4.81.1"
  }
}
